services:
  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_USER: ${WP_DB_USER}
      MARIADB_DATABASE: wp_db
      MARIADB_PASSWORD: ${WP_DB_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${WP_DB_ROOT_PASSWORD}
    volumes:
      - db:/var/lib/mysql

  memcached:
    image: memcached:latest
    restart: always

  wordpress-init:
    image: wordpress:cli-2.12-php8.4
    depends_on:
      db:
        condition: service_started
    restart: "no"
    user: root
    command: ["/bin/bash", "/tmp/init.sh"]
    environment:
      WP_DB_HOST: db
      WP_DB_USER: ${WP_DB_USER}
      WP_DB_PASSWORD: ${WP_DB_PASSWORD}
      WP_DB_NAME: wp_db
      WP_ADMIN_USER: ${WP_USERNAME}
      WP_ADMIN_PASSWORD: ${WP_PASSWORD}
      WP_TITLE: "New wordpress blog"
      WP_ADMIN_EMAIL: admin@example.com
      WP_LOCALE: ${WP_LOCALE}
      WP_INIT: "true"
      WP_URL: "${WP_SCHEME}://${WP_DOMAINNAME}"
      WORDPRESS_VERSION: ${WORDPRESS_VERSION}
      WP_CORE_AUTO_UPDATE: ${WP_CORE_AUTO_UPDATE}
    volumes:
      - wordpress:/var/www/html:rw
      - ./init.sh:/tmp/init.sh:ro
      - ./wordpress-alpine/cache-config.json:/tmp/cache-config.json:ro

  wordpress:
    image: ghcr.io/shesselink81/wordpress-apache:v7.0.4.2
    build:
      context: .
      args:
         - WP_DOMAINNAME=${WP_DOMAINNAME}
    restart: always
    user: www-data
    depends_on:
      db:
        condition: service_started
      wordpress-init:
        condition: service_completed_successfully
      memcached:
        condition: service_started
    ports:
      - "9080:80"
      - "9443:443"
    environment:
      WP_DB_HOST: db
      WP_DB_USER: ${WP_DB_USER}
      WP_DB_PASSWORD: ${WP_DB_PASSWORD}
      WP_DB_NAME: wp_db
      WP_DOMAINNAME: ${WP_DOMAINNAME}
    volumes:
      - wordpress:/var/www/html:rw

volumes:
  wordpress:
  db:
