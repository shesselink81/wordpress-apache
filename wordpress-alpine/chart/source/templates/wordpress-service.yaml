apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordpress-alpine.fullname" . }}-fpm
spec:
  selector:
    {{- include "wordpress-alpine.selectorLabels" . | nindent 4 }}-fpm
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP