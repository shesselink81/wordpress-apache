apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wordpress-alpine.fullname" . }}-init-config
data:
  init.sh: |-
{{ (.Files.Get "init-alpine.sh") | indent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wordpress-alpine.fullname" . }}-cache-config
data:
  cache-config.json: |-
{{ (.Files.Get "cache-config.json") | indent 4 }}